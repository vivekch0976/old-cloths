# OldCloths Monorepo Layout

The frontend application has been moved into:

- `frontend/` — Next.js + Tailwind marketplace UI

## Run frontend

```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

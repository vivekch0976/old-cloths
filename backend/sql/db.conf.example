# Copy to db.conf and update values for your environment.
# This file is sourced by run_sql.sh.

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=
DB_SCHEMA=app
DB_APP_USER=postgres
# Used only if SQL_PROMPT_PASSWORD=config.
DB_PASSWORD=
DB_BOOTSTRAP_NAME=postgres
